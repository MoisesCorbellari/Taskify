$kind: http-request
url: "{{Taskify}}/tasks/1"
method: GET
body:
  type: json
  content: |-
    import http.client
    import json

    conn = http.client.HTTPConnection("localhost", 8000)
    payload = "import http.client\n\nconn = http.client.HTTPConnection(\"localhost\", 8000)\npayload = ''\nheaders = {\n  'Cookie': 'Cookie_1=value'\n}\nconn.request(\"GET\", \"/ToDo_List/get-by-id/3\", payload, headers)\nres = conn.getresponse()\ndata = res.read()\nprint(data.decode(\"utf-8\"))"
    headers = {
      'Content-Type': 'application/json',
      'Cookie': 'Cookie_1=value'
    }
    conn.request("GET", "/ToDo_List/get-by-id/3", payload, headers)
    res = conn.getresponse()
    data = res.read()
    print(data.decode("utf-8"))
settings: {}
order: 3000
